import SingleCart from "./SingleCart";

const MenuFilterCards = () => {
  return (
    <div className="min-h-screen container bg-gray-100 py-12 px-4">
      <div className="max-w-md mx-auto grid">
        <SingleCart></SingleCart>
      </div>
    </div>
  );
};

export default MenuFilterCards;
